import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("SwiftUI використовує Struct")
                .font(.title)
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
